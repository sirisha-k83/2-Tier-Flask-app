    #    stage('Deploy') {
    #     steps {
    #        script {
    #        sh """
     #           # Using the modern 'docker compose' syntax
     #           docker-compose down || true
     #           docker-compose up -d
      #      """
       # }
    #}
#}